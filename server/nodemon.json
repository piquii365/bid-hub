{
  "watch": ["src"],
  "ext": "ts,js",
  "exec": "ts-node --project ./tsconfig.json ./src/index.ts",
  "ignore": ["src/**/*.spec.ts", "node_modules", "dist", "build", "public"],
  "env": {
    "NODE_ENV": "development"
  },
  "execMap": {
    "ts": "ts-node --project ./tsconfig.json"
  }
}
